---
// Batch 3A-6 — Blog Articles trash. Mirrors /admin/destinations/trash.astro.
import AdminLayout from '@/layouts/AdminLayout.astro';
import PageHeader from '@/components/admin/PageHeader.astro';
import DataTable from '@/components/admin/DataTable.astro';
import EmptyState from '@/components/admin/EmptyState.astro';
import ConfirmDeleteModal from '@/components/shared/ConfirmDeleteModal.astro';
import { listBlogs } from '@/services';
import { formatDate, publicStorageUrl } from '@/lib/helpers';

const supabase = Astro.locals.supabase;
const user = Astro.locals.user;
const searchParams = Astro.url.searchParams;

const q = searchParams.get('q') ?? '';
const page = Math.max(1, Number(searchParams.get('page') ?? 1) || 1);
const perPage = 10;

const { data: blogs, count, totalPages } = await listBlogs(supabase, {
  q: q || undefined,
  onlyDeleted: true,
  page,
  perPage,
});

const toast = searchParams.get('toast');
const toastType = searchParams.get('toastType') ?? 'success';
---

<AdminLayout title="Arsip Artikel Blog">
  <PageHeader title="Arsip Artikel Blog" crumbs={[{ label: 'Blog' }, { label: 'Articles', href: '/admin/blogs' }, { label: 'Arsip' }]}>
    <a href="/admin/blogs" class="btn btn-outline-secondary">
      <i class="bi bi-arrow-left me-1"></i> Kembali
    </a>
  </PageHeader>

  {toast && (
    <div class={`alert alert-${toastType === 'success' ? 'success' : 'danger'} d-flex align-items-center gap-2`}>
      <i class={`bi ${toastType === 'success' ? 'bi-check-circle' : 'bi-exclamation-triangle'}`}></i>
      {toast}
    </div>
  )}

  {blogs.length === 0 && !q ? (
    <div class="card">
      <div class="card-body">
        <EmptyState icon="bi bi-trash3" title="Arsip kosong" text="Artikel yang dihapus akan muncul di sini dan dapat dipulihkan kapan saja." />
      </div>
    </div>
  ) : (
    <DataTable
      columns={[
        { key: 'title', label: 'Artikel' },
        { key: 'category', label: 'Kategori' },
        { key: 'deleted_at', label: 'Diarsipkan' },
      ]}
      basePath="/admin/blogs/trash"
      searchParams={searchParams}
      searchPlaceholder="Cari artikel diarsipkan..."
      page={page}
      totalPages={totalPages}
      total={count}
      perPage={perPage}
      itemLabel="artikel"
      isEmpty={blogs.length === 0}
      emptyIcon="bi bi-search"
      emptyTitle="Tidak ditemukan"
      emptyText={`Tidak ada artikel diarsipkan yang cocok dengan "${q}".`}
      actionsLabel="Aksi"
    >
      {blogs.map((blog) => (
        <tr>
          <td>
            <div class="d-flex align-items-center gap-2">
              {blog.thumbnail ? (
                <img
                  src={publicStorageUrl(supabase, blog.thumbnail)}
                  alt={blog.title}
                  width="40"
                  height="40"
                  class="rounded-2"
                  style="object-fit:cover;"
                />
              ) : (
                <div class="rounded-2 bg-secondary-subtle d-flex align-items-center justify-content-center" style="width:40px;height:40px;">
                  <i class="bi bi-image text-secondary"></i>
                </div>
              )}
              <div class="fw-semibold text-muted">{blog.title}</div>
            </div>
          </td>
          <td>{blog.category?.name ?? '—'}</td>
          <td class="users-meta">{formatDate(blog.deleted_at)}</td>
          <td class="text-center">
            <div class="users-actions">
              <form method="post" action={`/api/admin/blogs/${blog.id}/restore`} class="d-inline">
                <button type="submit" class="users-action-btn" title="Pulihkan">
                  <i class="bi bi-arrow-counterclockwise"></i>
                </button>
              </form>
              {user?.role === 'admin' && (
                <button
                  type="button"
                  class="users-action-btn danger"
                  title="Hapus Permanen"
                  onclick={`openDeleteModal({ action: '/api/admin/blogs/${blog.id}?permanent=1', label: '${blog.title.replace(/'/g, "\\'")} (permanen)' })`}
                >
                  <i class="bi bi-trash3-fill"></i>
                </button>
              )}
            </div>
          </td>
        </tr>
      ))}
    </DataTable>
  )}

  <ConfirmDeleteModal />
</AdminLayout>
